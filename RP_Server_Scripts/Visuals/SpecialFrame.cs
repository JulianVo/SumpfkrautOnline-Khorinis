namespace RP_Server_Scripts.Visuals
{
    public enum SpecialFrame
    {
        // Draw Animation
        Draw = 0,

        // Attack Animation, Hit must be <= Combo!
        Combo = 1,
        Hit = 2,
        
        ItemHandle = 3,
    }
}
